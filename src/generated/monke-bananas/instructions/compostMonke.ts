/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { MONKE_BANANAS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const COMPOST_MONKE_DISCRIMINATOR = new Uint8Array([
  216, 158, 136, 82, 116, 172, 244, 117,
]);

export function getCompostMonkeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    COMPOST_MONKE_DISCRIMINATOR
  );
}

export type CompostMonkeInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountState extends string | AccountMeta<string> = string,
  TAccountMonkeBurn extends string | AccountMeta<string> = string,
  TAccountNftMint extends string | AccountMeta<string> = string,
  TAccountProgramVault extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      TAccountMonkeBurn extends string
        ? WritableAccount<TAccountMonkeBurn>
        : TAccountMonkeBurn,
      TAccountNftMint extends string
        ? ReadonlyAccount<TAccountNftMint>
        : TAccountNftMint,
      TAccountProgramVault extends string
        ? WritableAccount<TAccountProgramVault>
        : TAccountProgramVault,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CompostMonkeInstructionData = { discriminator: ReadonlyUint8Array };

export type CompostMonkeInstructionDataArgs = {};

export function getCompostMonkeInstructionDataEncoder(): FixedSizeEncoder<CompostMonkeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: COMPOST_MONKE_DISCRIMINATOR })
  );
}

export function getCompostMonkeInstructionDataDecoder(): FixedSizeDecoder<CompostMonkeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCompostMonkeInstructionDataCodec(): FixedSizeCodec<
  CompostMonkeInstructionDataArgs,
  CompostMonkeInstructionData
> {
  return combineCodec(
    getCompostMonkeInstructionDataEncoder(),
    getCompostMonkeInstructionDataDecoder()
  );
}

export type CompostMonkeAsyncInput<
  TAccountCaller extends string = string,
  TAccountState extends string = string,
  TAccountMonkeBurn extends string = string,
  TAccountNftMint extends string = string,
  TAccountProgramVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Anyone can compost — gets rent refund as incentive */
  caller: TransactionSigner<TAccountCaller>;
  state?: Address<TAccountState>;
  monkeBurn: Address<TAccountMonkeBurn>;
  /** NFT mint — verify supply == 0 (burned) */
  nftMint: Address<TAccountNftMint>;
  programVault?: Address<TAccountProgramVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCompostMonkeInstructionAsync<
  TAccountCaller extends string,
  TAccountState extends string,
  TAccountMonkeBurn extends string,
  TAccountNftMint extends string,
  TAccountProgramVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: CompostMonkeAsyncInput<
    TAccountCaller,
    TAccountState,
    TAccountMonkeBurn,
    TAccountNftMint,
    TAccountProgramVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CompostMonkeInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountMonkeBurn,
    TAccountNftMint,
    TAccountProgramVault,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    state: { value: input.state ?? null, isWritable: true },
    monkeBurn: { value: input.monkeBurn ?? null, isWritable: true },
    nftMint: { value: input.nftMint ?? null, isWritable: false },
    programVault: { value: input.programVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.state.value) {
    accounts.state.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([109, 111, 110, 107, 101, 95, 115, 116, 97, 116, 101])
        ),
      ],
    });
  }
  if (!accounts.programVault.value) {
    accounts.programVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            112, 114, 111, 103, 114, 97, 109, 95, 118, 97, 117, 108, 116,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.monkeBurn),
      getAccountMeta(accounts.nftMint),
      getAccountMeta(accounts.programVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCompostMonkeInstructionDataEncoder().encode({}),
    programAddress,
  } as CompostMonkeInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountMonkeBurn,
    TAccountNftMint,
    TAccountProgramVault,
    TAccountSystemProgram
  >);
}

export type CompostMonkeInput<
  TAccountCaller extends string = string,
  TAccountState extends string = string,
  TAccountMonkeBurn extends string = string,
  TAccountNftMint extends string = string,
  TAccountProgramVault extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Anyone can compost — gets rent refund as incentive */
  caller: TransactionSigner<TAccountCaller>;
  state: Address<TAccountState>;
  monkeBurn: Address<TAccountMonkeBurn>;
  /** NFT mint — verify supply == 0 (burned) */
  nftMint: Address<TAccountNftMint>;
  programVault: Address<TAccountProgramVault>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCompostMonkeInstruction<
  TAccountCaller extends string,
  TAccountState extends string,
  TAccountMonkeBurn extends string,
  TAccountNftMint extends string,
  TAccountProgramVault extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
>(
  input: CompostMonkeInput<
    TAccountCaller,
    TAccountState,
    TAccountMonkeBurn,
    TAccountNftMint,
    TAccountProgramVault,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CompostMonkeInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountState,
  TAccountMonkeBurn,
  TAccountNftMint,
  TAccountProgramVault,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MONKE_BANANAS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    state: { value: input.state ?? null, isWritable: true },
    monkeBurn: { value: input.monkeBurn ?? null, isWritable: true },
    nftMint: { value: input.nftMint ?? null, isWritable: false },
    programVault: { value: input.programVault ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.monkeBurn),
      getAccountMeta(accounts.nftMint),
      getAccountMeta(accounts.programVault),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCompostMonkeInstructionDataEncoder().encode({}),
    programAddress,
  } as CompostMonkeInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountState,
    TAccountMonkeBurn,
    TAccountNftMint,
    TAccountProgramVault,
    TAccountSystemProgram
  >);
}

export type ParsedCompostMonkeInstruction<
  TProgram extends string = typeof MONKE_BANANAS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can compost — gets rent refund as incentive */
    caller: TAccountMetas[0];
    state: TAccountMetas[1];
    monkeBurn: TAccountMetas[2];
    /** NFT mint — verify supply == 0 (burned) */
    nftMint: TAccountMetas[3];
    programVault: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CompostMonkeInstructionData;
};

export function parseCompostMonkeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCompostMonkeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      state: getNextAccount(),
      monkeBurn: getNextAccount(),
      nftMint: getNextAccount(),
      programVault: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCompostMonkeInstructionDataDecoder().decode(instruction.data),
  };
}
