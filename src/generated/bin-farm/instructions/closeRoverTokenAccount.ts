/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLOSE_ROVER_TOKEN_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  131, 235, 65, 77, 221, 30, 20, 139,
]);

export function getCloseRoverTokenAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ROVER_TOKEN_ACCOUNT_DISCRIMINATOR
  );
}

export type CloseRoverTokenAccountInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TAccountTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      TAccountTokenAccount extends string
        ? WritableAccount<TAccountTokenAccount>
        : TAccountTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseRoverTokenAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseRoverTokenAccountInstructionDataArgs = {};

export function getCloseRoverTokenAccountInstructionDataEncoder(): FixedSizeEncoder<CloseRoverTokenAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLOSE_ROVER_TOKEN_ACCOUNT_DISCRIMINATOR,
    })
  );
}

export function getCloseRoverTokenAccountInstructionDataDecoder(): FixedSizeDecoder<CloseRoverTokenAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseRoverTokenAccountInstructionDataCodec(): FixedSizeCodec<
  CloseRoverTokenAccountInstructionDataArgs,
  CloseRoverTokenAccountInstructionData
> {
  return combineCodec(
    getCloseRoverTokenAccountInstructionDataEncoder(),
    getCloseRoverTokenAccountInstructionDataDecoder()
  );
}

export type CloseRoverTokenAccountAsyncInput<
  TAccountCaller extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Anyone can call — permissionless */
  caller: TransactionSigner<TAccountCaller>;
  roverAuthority?: Address<TAccountRoverAuthority>;
  /** Token account to close — must be owned by rover_authority */
  tokenAccount: Address<TAccountTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export async function getCloseRoverTokenAccountInstructionAsync<
  TAccountCaller extends string,
  TAccountRoverAuthority extends string,
  TAccountTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: CloseRoverTokenAccountAsyncInput<
    TAccountCaller,
    TAccountRoverAuthority,
    TAccountTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseRoverTokenAccountInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority,
    TAccountTokenAccount,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.tokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseRoverTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseRoverTokenAccountInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority,
    TAccountTokenAccount,
    TAccountTokenProgram
  >);
}

export type CloseRoverTokenAccountInput<
  TAccountCaller extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Anyone can call — permissionless */
  caller: TransactionSigner<TAccountCaller>;
  roverAuthority: Address<TAccountRoverAuthority>;
  /** Token account to close — must be owned by rover_authority */
  tokenAccount: Address<TAccountTokenAccount>;
  tokenProgram: Address<TAccountTokenProgram>;
};

export function getCloseRoverTokenAccountInstruction<
  TAccountCaller extends string,
  TAccountRoverAuthority extends string,
  TAccountTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: CloseRoverTokenAccountInput<
    TAccountCaller,
    TAccountRoverAuthority,
    TAccountTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CloseRoverTokenAccountInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountRoverAuthority,
  TAccountTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.tokenAccount),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseRoverTokenAccountInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseRoverTokenAccountInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority,
    TAccountTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedCloseRoverTokenAccountInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can call — permissionless */
    caller: TAccountMetas[0];
    roverAuthority: TAccountMetas[1];
    /** Token account to close — must be owned by rover_authority */
    tokenAccount: TAccountMetas[2];
    tokenProgram: TAccountMetas[3];
  };
  data: CloseRoverTokenAccountInstructionData;
};

export function parseCloseRoverTokenAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseRoverTokenAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      roverAuthority: getNextAccount(),
      tokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getCloseRoverTokenAccountInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
