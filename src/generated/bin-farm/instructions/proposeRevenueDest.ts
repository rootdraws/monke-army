/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const PROPOSE_REVENUE_DEST_DISCRIMINATOR = new Uint8Array([
  66, 201, 224, 148, 215, 200, 177, 47,
]);

export function getProposeRevenueDestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_REVENUE_DEST_DISCRIMINATOR
  );
}

export type ProposeRevenueDestInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeRevenueDestInstructionData = {
  discriminator: ReadonlyUint8Array;
  newRevenueDest: Address;
};

export type ProposeRevenueDestInstructionDataArgs = { newRevenueDest: Address };

export function getProposeRevenueDestInstructionDataEncoder(): FixedSizeEncoder<ProposeRevenueDestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newRevenueDest', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PROPOSE_REVENUE_DEST_DISCRIMINATOR })
  );
}

export function getProposeRevenueDestInstructionDataDecoder(): FixedSizeDecoder<ProposeRevenueDestInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newRevenueDest', getAddressDecoder()],
  ]);
}

export function getProposeRevenueDestInstructionDataCodec(): FixedSizeCodec<
  ProposeRevenueDestInstructionDataArgs,
  ProposeRevenueDestInstructionData
> {
  return combineCodec(
    getProposeRevenueDestInstructionDataEncoder(),
    getProposeRevenueDestInstructionDataDecoder()
  );
}

export type ProposeRevenueDestAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  roverAuthority?: Address<TAccountRoverAuthority>;
  newRevenueDest: ProposeRevenueDestInstructionDataArgs['newRevenueDest'];
};

export async function getProposeRevenueDestInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ProposeRevenueDestAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ProposeRevenueDestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getProposeRevenueDestInstructionDataEncoder().encode(
      args as ProposeRevenueDestInstructionDataArgs
    ),
    programAddress,
  } as ProposeRevenueDestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority
  >);
}

export type ProposeRevenueDestInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  roverAuthority: Address<TAccountRoverAuthority>;
  newRevenueDest: ProposeRevenueDestInstructionDataArgs['newRevenueDest'];
};

export function getProposeRevenueDestInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ProposeRevenueDestInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): ProposeRevenueDestInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountRoverAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getProposeRevenueDestInstructionDataEncoder().encode(
      args as ProposeRevenueDestInstructionDataArgs
    ),
    programAddress,
  } as ProposeRevenueDestInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority
  >);
}

export type ParsedProposeRevenueDestInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    roverAuthority: TAccountMetas[2];
  };
  data: ProposeRevenueDestInstructionData;
};

export function parseProposeRevenueDestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedProposeRevenueDestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      roverAuthority: getNextAccount(),
    },
    data: getProposeRevenueDestInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
