/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CLAIM_POOL_FEES_DISCRIMINATOR = new Uint8Array([
  33, 187, 125, 186, 41, 247, 236, 89,
]);

export function getClaimPoolFeesDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_POOL_FEES_DISCRIMINATOR
  );
}

export type ClaimPoolFeesInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimPoolFeesInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClaimPoolFeesInstructionDataArgs = {};

export function getClaimPoolFeesInstructionDataEncoder(): FixedSizeEncoder<ClaimPoolFeesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_POOL_FEES_DISCRIMINATOR })
  );
}

export function getClaimPoolFeesInstructionDataDecoder(): FixedSizeDecoder<ClaimPoolFeesInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimPoolFeesInstructionDataCodec(): FixedSizeCodec<
  ClaimPoolFeesInstructionDataArgs,
  ClaimPoolFeesInstructionData
> {
  return combineCodec(
    getClaimPoolFeesInstructionDataEncoder(),
    getClaimPoolFeesInstructionDataDecoder()
  );
}

export type ClaimPoolFeesAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  /** Anyone can crank this instruction */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  roverAuthority?: Address<TAccountRoverAuthority>;
};

export async function getClaimPoolFeesInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ClaimPoolFeesAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ClaimPoolFeesInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getClaimPoolFeesInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimPoolFeesInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority
  >);
}

export type ClaimPoolFeesInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  /** Anyone can crank this instruction */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  roverAuthority: Address<TAccountRoverAuthority>;
};

export function getClaimPoolFeesInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ClaimPoolFeesInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): ClaimPoolFeesInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountRoverAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getClaimPoolFeesInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimPoolFeesInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority
  >);
}

export type ParsedClaimPoolFeesInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can crank this instruction */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    roverAuthority: TAccountMetas[2];
  };
  data: ClaimPoolFeesInstructionData;
};

export function parseClaimPoolFeesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedClaimPoolFeesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      roverAuthority: getNextAccount(),
    },
    data: getClaimPoolFeesInstructionDataDecoder().decode(instruction.data),
  };
}
