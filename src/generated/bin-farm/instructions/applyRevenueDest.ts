/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPLY_REVENUE_DEST_DISCRIMINATOR = new Uint8Array([
  112, 217, 50, 135, 51, 128, 208, 199,
]);

export function getApplyRevenueDestDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_REVENUE_DEST_DISCRIMINATOR
  );
}

export type ApplyRevenueDestInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyRevenueDestInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyRevenueDestInstructionDataArgs = {};

export function getApplyRevenueDestInstructionDataEncoder(): FixedSizeEncoder<ApplyRevenueDestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPLY_REVENUE_DEST_DISCRIMINATOR })
  );
}

export function getApplyRevenueDestInstructionDataDecoder(): FixedSizeDecoder<ApplyRevenueDestInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyRevenueDestInstructionDataCodec(): FixedSizeCodec<
  ApplyRevenueDestInstructionDataArgs,
  ApplyRevenueDestInstructionData
> {
  return combineCodec(
    getApplyRevenueDestInstructionDataEncoder(),
    getApplyRevenueDestInstructionDataDecoder()
  );
}

export type ApplyRevenueDestAsyncInput<
  TAccountCaller extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  roverAuthority?: Address<TAccountRoverAuthority>;
};

export async function getApplyRevenueDestInstructionAsync<
  TAccountCaller extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ApplyRevenueDestAsyncInput<TAccountCaller, TAccountRoverAuthority>,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ApplyRevenueDestInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getApplyRevenueDestInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyRevenueDestInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority
  >);
}

export type ApplyRevenueDestInput<
  TAccountCaller extends string = string,
  TAccountRoverAuthority extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  roverAuthority: Address<TAccountRoverAuthority>;
};

export function getApplyRevenueDestInstruction<
  TAccountCaller extends string,
  TAccountRoverAuthority extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: ApplyRevenueDestInput<TAccountCaller, TAccountRoverAuthority>,
  config?: { programAddress?: TProgramAddress }
): ApplyRevenueDestInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountRoverAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.roverAuthority),
    ],
    data: getApplyRevenueDestInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyRevenueDestInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountRoverAuthority
  >);
}

export type ParsedApplyRevenueDestInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    roverAuthority: TAccountMetas[1];
  };
  data: ApplyRevenueDestInstructionData;
};

export function parseApplyRevenueDestInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApplyRevenueDestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), roverAuthority: getNextAccount() },
    data: getApplyRevenueDestInstructionDataDecoder().decode(instruction.data),
  };
}
