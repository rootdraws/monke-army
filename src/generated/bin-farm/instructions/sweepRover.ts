/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const SWEEP_ROVER_DISCRIMINATOR = new Uint8Array([
  199, 22, 58, 214, 89, 187, 187, 249,
]);

export function getSweepRoverDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SWEEP_ROVER_DISCRIMINATOR);
}

export type SweepRoverInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TAccountRevenueDest extends string | AccountMeta<string> = string,
  TAccountBotDest extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      TAccountRevenueDest extends string
        ? WritableAccount<TAccountRevenueDest>
        : TAccountRevenueDest,
      TAccountBotDest extends string
        ? WritableAccount<TAccountBotDest>
        : TAccountBotDest,
      ...TRemainingAccounts,
    ]
  >;

export type SweepRoverInstructionData = { discriminator: ReadonlyUint8Array };

export type SweepRoverInstructionDataArgs = {};

export function getSweepRoverInstructionDataEncoder(): FixedSizeEncoder<SweepRoverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SWEEP_ROVER_DISCRIMINATOR })
  );
}

export function getSweepRoverInstructionDataDecoder(): FixedSizeDecoder<SweepRoverInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSweepRoverInstructionDataCodec(): FixedSizeCodec<
  SweepRoverInstructionDataArgs,
  SweepRoverInstructionData
> {
  return combineCodec(
    getSweepRoverInstructionDataEncoder(),
    getSweepRoverInstructionDataDecoder()
  );
}

export type SweepRoverAsyncInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountRevenueDest extends string = string,
  TAccountBotDest extends string = string,
> = {
  /** Anyone can call sweep — permissionless */
  caller: TransactionSigner<TAccountCaller>;
  config?: Address<TAccountConfig>;
  roverAuthority?: Address<TAccountRoverAuthority>;
  revenueDest: Address<TAccountRevenueDest>;
  botDest: Address<TAccountBotDest>;
};

export async function getSweepRoverInstructionAsync<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountRevenueDest extends string,
  TAccountBotDest extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: SweepRoverAsyncInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountRevenueDest,
    TAccountBotDest
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SweepRoverInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountRevenueDest,
    TAccountBotDest
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    revenueDest: { value: input.revenueDest ?? null, isWritable: true },
    botDest: { value: input.botDest ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.revenueDest),
      getAccountMeta(accounts.botDest),
    ],
    data: getSweepRoverInstructionDataEncoder().encode({}),
    programAddress,
  } as SweepRoverInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountRevenueDest,
    TAccountBotDest
  >);
}

export type SweepRoverInput<
  TAccountCaller extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountRevenueDest extends string = string,
  TAccountBotDest extends string = string,
> = {
  /** Anyone can call sweep — permissionless */
  caller: TransactionSigner<TAccountCaller>;
  config: Address<TAccountConfig>;
  roverAuthority: Address<TAccountRoverAuthority>;
  revenueDest: Address<TAccountRevenueDest>;
  botDest: Address<TAccountBotDest>;
};

export function getSweepRoverInstruction<
  TAccountCaller extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountRevenueDest extends string,
  TAccountBotDest extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: SweepRoverInput<
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountRevenueDest,
    TAccountBotDest
  >,
  config?: { programAddress?: TProgramAddress }
): SweepRoverInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountConfig,
  TAccountRoverAuthority,
  TAccountRevenueDest,
  TAccountBotDest
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: true },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    revenueDest: { value: input.revenueDest ?? null, isWritable: true },
    botDest: { value: input.botDest ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.revenueDest),
      getAccountMeta(accounts.botDest),
    ],
    data: getSweepRoverInstructionDataEncoder().encode({}),
    programAddress,
  } as SweepRoverInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountRevenueDest,
    TAccountBotDest
  >);
}

export type ParsedSweepRoverInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can call sweep — permissionless */
    caller: TAccountMetas[0];
    config: TAccountMetas[1];
    roverAuthority: TAccountMetas[2];
    revenueDest: TAccountMetas[3];
    botDest: TAccountMetas[4];
  };
  data: SweepRoverInstructionData;
};

export function parseSweepRoverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSweepRoverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      config: getNextAccount(),
      roverAuthority: getNextAccount(),
      revenueDest: getNextAccount(),
      botDest: getNextAccount(),
    },
    data: getSweepRoverInstructionDataDecoder().decode(instruction.data),
  };
}
