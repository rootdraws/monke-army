/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { BIN_FARM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INITIALIZE_ROVER_DISCRIMINATOR = new Uint8Array([
  48, 101, 239, 119, 204, 154, 97, 18,
]);

export function getInitializeRoverDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_ROVER_DISCRIMINATOR
  );
}

export type InitializeRoverInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoverAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoverAuthority extends string
        ? WritableAccount<TAccountRoverAuthority>
        : TAccountRoverAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeRoverInstructionData = {
  discriminator: ReadonlyUint8Array;
  revenueDest: Address;
};

export type InitializeRoverInstructionDataArgs = { revenueDest: Address };

export function getInitializeRoverInstructionDataEncoder(): FixedSizeEncoder<InitializeRoverInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['revenueDest', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_ROVER_DISCRIMINATOR })
  );
}

export function getInitializeRoverInstructionDataDecoder(): FixedSizeDecoder<InitializeRoverInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['revenueDest', getAddressDecoder()],
  ]);
}

export function getInitializeRoverInstructionDataCodec(): FixedSizeCodec<
  InitializeRoverInstructionDataArgs,
  InitializeRoverInstructionData
> {
  return combineCodec(
    getInitializeRoverInstructionDataEncoder(),
    getInitializeRoverInstructionDataDecoder()
  );
}

export type InitializeRoverAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  roverAuthority?: Address<TAccountRoverAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  revenueDest: InitializeRoverInstructionDataArgs['revenueDest'];
};

export async function getInitializeRoverInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: InitializeRoverAsyncInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeRoverInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }
  if (!accounts.roverAuthority.value) {
    accounts.roverAuthority.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            114, 111, 118, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116,
            121,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRoverInstructionDataEncoder().encode(
      args as InitializeRoverInstructionDataArgs
    ),
    programAddress,
  } as InitializeRoverInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeRoverInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
  TAccountRoverAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  roverAuthority: Address<TAccountRoverAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  revenueDest: InitializeRoverInstructionDataArgs['revenueDest'];
};

export function getInitializeRoverInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TAccountRoverAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof BIN_FARM_PROGRAM_ADDRESS,
>(
  input: InitializeRoverInput<
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeRoverInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig,
  TAccountRoverAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? BIN_FARM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    roverAuthority: { value: input.roverAuthority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.roverAuthority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeRoverInstructionDataEncoder().encode(
      args as InitializeRoverInstructionDataArgs
    ),
    programAddress,
  } as InitializeRoverInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig,
    TAccountRoverAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeRoverInstruction<
  TProgram extends string = typeof BIN_FARM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
    roverAuthority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitializeRoverInstructionData;
};

export function parseInitializeRoverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedInitializeRoverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      config: getNextAccount(),
      roverAuthority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeRoverInstructionDataDecoder().decode(instruction.data),
  };
}
