# ============ FRONTEND ============

# Solana RPC endpoint — DO NOT use public RPC in production
# Public endpoint rate-limits aggressively, will break bot and priority fee lookups
# Get a free/paid endpoint from Helius (helius.dev) or Quicknode
VITE_RPC_URL=https://api.mainnet-beta.solana.com

# Fee in basis points (30 = 0.3%)
VITE_FEE_BPS=30

# Birdeye API key (for token pricing)
VITE_BIRDEYE_API_KEY=

# ============ PROGRAMS (update after deployment) ============

# monke.army core program ID
CORE_PROGRAM_ID=
# monke_bananas program ID (burn $BANANAS, feed monkes, claim SOL)
MONKE_BANANAS_PROGRAM_ID=

# ============ BOT ============

# RPC endpoint for bot (dedicated endpoint recommended)
RPC_URL=https://api.mainnet-beta.solana.com

# Geyser/gRPC endpoint (Helius LaserStream recommended)
# Required for real-time price monitoring
GRPC_ENDPOINT=

# Bot wallet — must match config.bot on core program
# WARNING: Never commit this file with a real key. Use BOT_KEYPAIR_PATH for production.
# Option 1: Base58 private key (dev only)
BOT_PRIVATE_KEY=
# Option 2: Path to Solana CLI keypair JSON file (recommended for production)
BOT_KEYPAIR_PATH=

# $BANANAS token mint address
BANANAS_MINT=

# Keeper check interval (ms, default 1hr — adaptive: 30s during Saturday processing)
KEEPER_CHECK_INTERVAL_MS=3600000

# ============ DAMM v2 POOL (set after pool creation via meteora-invent) ============

# DAMM v2 $BANANAS/SOL pool address
DAMM_V2_POOL=
# DAMM v2 position address (LP position held by rover_authority)
DAMM_V2_POSITION=
# DAMM v2 position NFT mint (transferred to rover_authority)
DAMM_V2_POSITION_NFT=
# Pool token A mint ($BANANAS)
DAMM_V2_TOKEN_A_MINT=
# Pool token B mint (SOL — usually So11111111111111111111111111111111111111112)
DAMM_V2_TOKEN_B_MINT=So11111111111111111111111111111111111111112
# Pool token A vault
DAMM_V2_TOKEN_A_VAULT=
# Pool token B vault
DAMM_V2_TOKEN_B_VAULT=
