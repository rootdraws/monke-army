# monke.army harvest bot environment

# Solana RPC (use a paid RPC for production — Helius, Triton, etc.)
RPC_URL=https://api.mainnet-beta.solana.com

# Geyser/gRPC endpoint (Helius LaserStream recommended)
# Required for real-time price monitoring. Without this, bot falls back to polling.
# Helius LaserStream: https://laserstream-mainnet.helius-rpc.com?api-key=YOUR_KEY
GRPC_ENDPOINT=

# Bot wallet — must match config.bot on core program
# WARNING: Never commit this file with a real key. Use BOT_KEYPAIR_PATH for production.
# Option 1: Base58 private key (dev only)
BOT_PRIVATE_KEY=
# Option 2: Path to Solana CLI keypair JSON file (recommended for production)
BOT_KEYPAIR_PATH=

# Program IDs (update after deployment)
CORE_PROGRAM_ID=
MONKE_BANANAS_PROGRAM_ID=

# $BANANAS token mint address
BANANAS_MINT=

# Keeper check interval (ms, default 1hr — adaptive: 30s during Saturday processing)
KEEPER_CHECK_INTERVAL_MS=3600000

# Jupiter API endpoint (default: https://lite-api.jup.ag — upgrade for production)
JUPITER_API_URL=https://lite-api.jup.ag

# Health check HTTP port (default: 8080)
HEALTH_PORT=8080

# Delay between pool checks in safety poll (ms, default: 200)
SAFETY_POLL_DELAY_MS=200

# Position cache file path (default: ./positions-cache.json)
CACHE_PATH=./positions-cache.json

# Skip positions with fewer bins (default: 2, anti-griefing)
MIN_POSITION_BINS=2

# Skip positions below this SOL value (lamports)
MIN_POSITION_VALUE=

# Minimum swap output in lamports (default: 5000000 = ~0.005 SOL)
MIN_SWAP_LAMPORTS=5000000

# Auto-trigger deposit_sol when dist_pool exceeds this threshold (lamports, default: 0.5 SOL)
DEPOSIT_SOL_THRESHOLD_LAMPORTS=500000000

# Log level (default: info — options: trace, debug, info, warn, error, fatal)
LOG_LEVEL=info
